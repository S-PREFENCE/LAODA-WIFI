# 劳大象棋 · 联机对战 方案审核（v0 待批）

> 状态：方案评审稿，未开始写代码。等用户审批后，再在 `劳大联机/` 落地。

## 1. 背景与硬约束
- 游戏：劳大象棋网页版（纯前端，已部署 GitHub Pages，单机 / AI / 残局完备）。
- 目标：新增「双人联机对战」，两台设备在内网实时对弈。
- **网络约束（用户确认）**：华为内部网络，**不能连接外部网络**。
  → 所有依赖公网的服务（Railway / CF Worker / Supabase / Firebase / PeerJS 云信令）一律不可用；GitHub Pages 外部也可能不可达，前端不能依赖它。
- **设备约束**：玩家可在内网任意设备间自由切换，用**房间码**配对。
- **本次明确：不做 exe 封装。**

## 2. 目标（v1）
- 内网任意两台设备，浏览器打开同一地址 + 同一房间码 → 实时双人下棋。
- 落子实时同步、轮流传棋、将死判负、音效 / 打击感照旧。
- **不改动现有单机 / AI / 残局逻辑**（独立模块，单机备份版不受影响）。

## 3. 架构拓扑
```
        ┌──────────────┐      WebSocket(棋步JSON)       ┌──────────────┐
        │ 玩家A 浏览器  │ <───────┐              ┌──────> │ 玩家B 浏览器  │
        └──────────────┘         │              │        └──────────────┘
                                 ▼              ▲
                        ┌──────────────────────────┐
                        │   内网主机（单机进程）      │
                        │   · 静态托管前端页面        │
                        │   · WebSocket 中继         │
                        │   · 房间码配对             │
                        └──────────────────────────┘
                     仅一台机器运行；玩家端纯浏览器、零安装
```
- 唯一固定点：一台内网机器跑服务进程（「棋桌」）。玩家端不需要装任何东西。

## 4. 技术方案选型（重点：不做 exe，且内网不能联网装包）

### 4.1 为什么排除带依赖的方案
- `npm i ws` / `pip install websockets` 都要访问**外部软件源**（npmjs / PyPI），而内网出不了外网 → 在主机上安装会失败。
- 因此**必须零依赖**：只用运行时自带的标准库实现最小 WebSocket。

### 4.2 可行方案对比
| 方案 | 额外依赖 | 主机需装 | 评价 |
|---|---|---|---|
| ❌ exe 封装 | 编译产物 | 无（双击） | 用户已否 |
| ❌ Node + ws | npm 包 | Node + 联网装包 | 内网装不了 |
| ❌ Python + websockets | pip 包 | Python + 联网装包 | 内网装不了 |
| ✅ **Node 标准库零依赖** | 无 | 仅需 Node | 推荐 |
| ✅ **Python 标准库零依赖** | 无 | 仅需 Python | 推荐 |
| · .bat / .ps1 启动器 | 无 | 同上 | 便捷包装（非 exe） |

### 4.3 推荐：双版本零依赖服务（Node + Python 各一份）
- 自己用标准库手写**最小 WebSocket**（只处理文本帧，足够传 `{from,to}` JSON）：
  - Node：`http` + `crypto`（SHA1 + magic 算 accept，帧编解码）。
  - Python：`http.server` + `hashlib` + `base64` + `socket`。
- 前端 `net.js` 用浏览器原生 `WebSocket`，**零依赖**。
- **交付两份启动脚本**：`server.js`（Node）与 `server.py`（Python）。主机只要有其中之一即可运行，**无需联网安装任何东西**。
- 附 `start.bat`（Windows 双击调用 node/python），仅为包装、非 exe 编译。

> 说明：主机仍需有 Node 或 Python 运行时。若内网完全无法获取运行时，则从可联网机器拷贝运行时目录（罕见，先标记风险）。

## 5. 服务端设计
- **房间码**：6 位字典码（去歧义字符）。「创建房间」→ 生成码并等待；「加入房间」→ 校验码存在且未满（2 人）→ 配对。
- **中继 vs 权威**：推荐**轻度权威**——服务端保存房间棋盘，每步用现有规则引擎校验合法性后再广播，防两端不同步 / 误传。多几十行，显著更稳。
- **端口**：默认 `3000`，可命令行参数改；前端与中继同端口同源，规避跨域。
- **断线**：玩家断开 → 通知对手「对方掉线」；可凭房间码 + 角色重连回原房间（v1 先支持提示，重连作为可选）。
- **静态托管**：服务端顺带托管 `index.html` / `css` / `js` / `assets`，前端不依赖 GitHub Pages。

## 6. 前端改动（最小侵入）
- 新增「联机对战」模式按钮（与现有 双人 / AI / 残局 并列）。
- 新增 `net.js`：封装 `WebSocket` 连接、创建 / 加入房间、发送 / 接收棋步（统一接口，未来换传输层也容易）。
- `game.js`：联机模式下，本地落子 → `net.send(move)`；收到 `net.onMove` → 喂给现有 `XQ.Game.applyMove()`（复用现有动画 / 音效 / 将死判定）。**不碰 AI、残局、单机逻辑**。
- 联机版代码放在独立文件夹 `劳大联机/`，单机版（`news-象棋` 已本地备份）不受影响。

## 7. v1 功能范围（待勾选）
- 必做：房间码创建 / 加入、实时落子同步、轮次校验、将死 / 和棋判定、断线提示。
- 可选：断线重连、聊天、观战、联机悔棋（需双方协商，建议 v1 不做或仅本地预览）。

## 8. 主机如何运行（给用户的最简步骤）
1. 把 `劳大联机/` 整文件夹拷到内网某台 Windows 机器（该机有 Node 或 Python）。
2. 双击 `start.bat`（或命令行 `node server.js` / `python server.py`）。
3. 程序打印本机内网 IP（如 `http://192.168.1.50:3000`）。
4. 两台设备浏览器打开该地址 → 一方创建房间拿码、另一方输码 → 开下。
5. 首次需放行 Windows 防火墙入站 3000 端口（脚本会提示或给命令）。

## 9. 风险与缓解
- **内网互通性**：跨 VLAN 可能 ping 不通 → 上线前 `ping` 验证；不行则改走同一网段 / 指定主机。
- **运行时缺失**：主机无 Node / Python → 从可联网机器拷贝运行时；双版本交付降低概率。
- **防火墙**：首次入站被拦 → 提供一键放行命令。
- **不同步 / 作弊**：轻度权威校验兜底。

## 10. 待你审批的决策点
1. 服务端语言：**Node 零依赖 / Python 零依赖 / 两者都给（推荐）**？
2. 服务端角色：**哑中继 / 轻度权威（推荐）**？
3. v1 功能：必做项外，**重连 / 聊天 / 观战 / 悔棋** 要不要（多选）？
4. 主机方式：**固定常开机 / 临时启动(.bat) / 两者都支持（推荐）**？
5. 是否同意我据此在 `劳大联机/` 写代码？

---
*本文件为方案评审稿。审批通过前，`劳大联机/` 仅含本文档，不含游戏代码。*
