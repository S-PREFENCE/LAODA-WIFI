# 劳大象棋 · 联机对战 —— 傻瓜部署教程

> 目标：公司内网两台设备实时下棋。**不用装软件、不用连外网、数据不出公司内网。**
> 全程只需 3 步：选一台电脑当"棋桌" → 双击启动 → 两个人打开网址对战。

---

## 一、一句话原理（看懂这段就够了）

象棋游戏本身是个网页，但"两个人实时对下"需要有台机器在中间**传每一步棋**。
我们让公司内网里**任意一台电脑**临时充当这台"中转机"（叫它**主机 / 棋桌**）：

```
  玩家A的浏览器  ──┐                 ┌──  玩家B的浏览器
   (手机/电脑)    ──┼──►  主机(棋桌)  ──┼──►  (手机/电脑)
  创建房间拿码    ──┘   传每一步棋    ──┘   输码加入
```

- 主机只干两件事：**把游戏页面发给你** + **把你走的每一步转发给对手**。
- 棋步数据只在你们内网三台设备之间转，**不经过任何外网服务器**。
- 玩家设备（你的手机/电脑）**零安装**，有个浏览器就行。

---

## 二、你需要准备什么（极少）

| 角色 | 需要什么 | 难度 |
|------|----------|------|
| **主机（棋桌）** | 一台连了公司内网的 Windows 电脑，装了 **Node** 或 **Python**（有其一即可） | 一次性，5 分钟 |
| **玩家A** | 任意设备 + 浏览器，能访问主机内网地址 | 无 |
| **玩家B** | 任意设备 + 浏览器，能访问主机内网地址 | 无 |

> 怎么看主机有没有 Node / Python？
> 在主机上按 `Win+R` → 输入 `cmd` 回车 → 黑框里分别输：
> - `node -v` 回车 → 显示 `v18.x.x` 之类就是有 Node
> - `python --version` 回车 → 显示 `Python 3.x.x` 之类就是有 Python
> 两个里**任意一个**有就行。都没有？见文末"万一主机啥都没装"。

---

## 三、第 1 步：选主机 + 启动（最简单方式 ⭐）

1. 在主机上，把 `劳大联机` 这个文件夹拷过去（U盘、内网共享、或 git 拉下来都行）。
2. 进到 `劳大联机` 文件夹，**双击 `start.bat`**。
   - 它会自动检测：有 Node 就用 Node，没有就试 Python，然后帮你把服务跑起来。
3. 弹出黑色窗口，**别关**。你会看到类似这样一行：
   ```
   本机访问：  http://127.0.0.1:3000/
   内网访问：  http://192.168.1.50:3000/   ← 把这个网址发给同事
   ```
   > 注意 `192.168.x.x` 或 `10.x.x.x` 那行，那就是"棋桌地址"。

**搞定。** 主机这边就做完了，保持这个黑窗口开着就行。

> 备选（不用双击，手动）：在 `劳大联机` 文件夹地址栏输入 `cmd` 回车，然后：
> - 有 Node：`node server.js`
> - 有 Python：`python server.py`
> 效果一样。

---

## 四、第 2 步：两个玩家怎么连

1. **玩家A**（建议就用那台主机本身，或另一台设备）打开浏览器，输入**棋桌地址**
   （比如 `http://192.168.1.50:3000/`）。
2. 在左侧模式里点 **「联机」**，然后点 **「创建房间」**。
3. 屏幕显示一个 **6 位房间码**（如 `A1B2C3`）。**把这个码发给玩家B**。
4. **玩家B** 在自己设备浏览器打开同一个棋桌地址 → 点「联机」→ 点 **「加入房间」** → 输入那 6 位码 → 确定。
5. 两人屏幕同时出现棋盘，**红方先走**，开下！

> 换设备？随时换。关掉这边的浏览器，去另一台设备打开同一地址、输同一房间码（或新开一局）就行。房间码就是用来"配对"的，不挑设备。

---

## 五、手机能玩吗？

能。只要手机连的是**同一个公司 WiFi / 内网**，浏览器打开棋桌地址即可。
（如果手机连的是 4G/5G 外网，那就连不到内网主机，这是正常的——本就是内网对战。）

---

## 六、常见坑（连不上先看这里）

**① 打开地址显示"无法访问 / 拒绝连接"**
- 检查主机那个黑窗口**是不是还开着**（关了所有人都掉线）。
- 检查地址有没有抄错（是 `192.168.x.x:3000`，不是 `127.0.0.1`——`127.0.0.1` 只能主机自己用）。

**② Windows 防火墙弹窗"是否允许访问"**
- 主机第一次启动时会弹，**一定要点「允许访问 / 允许」**（勾上"专用网络"）。
- 如果之前手滑点了取消：控制面板 → Windows Defender 防火墙 → 允许应用通过防火墙 → 找到 `Node.js` 或 `Python` → 勾选"专用"→ 确定。

**③ 同办公室但 ping 不通主机**
- 公司内网可能按部门/楼层做了隔离，A 访问不了 B。这种情况需要找 IT 确认两台机器是否在同一网段，或换一台"大家都能访问"的机器当主机（比如公共服务器、或某台网关机）。

**④ 端口 3000 被占用**
- 主机上若已有程序占了 3000，启动会报错。换端口：用命令行启动并加端口号，如 `node server.js 8080`，然后地址变成 `http://192.168.x.x:8080/`。

---

## 七、不想玩了 / 收工

- 关掉主机上那个黑色窗口 = 服务停止，所有人自动断开。
- 下次想玩，再双击 `start.bat` 即可，地址可能变（换了 WiFi/网段），以黑窗口新打印的为准。

---

## 八、安全说明（给公司信息安全同事看）

- 棋步数据（`{从哪走到哪}`）只在 **主机 ↔ 两名玩家** 这条内网链路里流转，**不发任何外网请求、不上云**。
- 前端游戏页面**完全自包含**，无任何 CDN、外部字体、外部 JS 引用（已逐文件核查）。
- 服务端代码仅几十行、纯标准库、可逐行审计，便于安全审查。

---

## 附：万一主机啥都没装（Node / Python 都没有）

内网一般装不了外网软件。两个办法：
1. **换一台有 Node 或 Python 的同事电脑当主机**（推荐，零安装）。
2. 如果公司允许，在能连外网的机器上提前下载好 Node 安装包（官网 nodejs.org，选 LTS 版 `.msi`），拷进内网主机双击安装，再走上面的步骤。

---

## 附：技术原理（给好奇的人）

- `server.js` / `server.py`：用运行时自带标准库手写的最小 WebSocket 服务，同时托管前端静态文件和转发棋步（不引入任何第三方包，所以内网离线也能跑）。
- `js/net.js`：浏览器里用原生 WebSocket 连同源地址，把本地走子发给主机、把远端走子落回棋盘。
- 服务端做"轻量权威"：每步校验走子方颜色与轮次、拒绝非法走法，避免两端棋盘不同步。
- 房间码 = 服务端内存里的一个对局房间 ID，两人输同一个码即被配成一对。

教程结束。有任何一步卡住，把现象（或截图）发给我，我直接定位。
